#include "main.h"
/**
 * main - block
 * Description: print alphabet in lower case
 * Return: 0
 */
int main(void)
{
	char ch = "a";

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
return (0);
}
